# Deploy instructions for bot chats

## Deploy to GitHub + HuggingFace
Run from project root:
```
git add -A
git commit -m "message"
git push origin main
git push hf main
```

Or double-click `deploy.bat` (optional commit message as argument).

## Remotes
- GitHub: origin (https://github.com/hoangitsk/yniemdienanh)
- HuggingFace: hf (https://huggingface.co/spaces/Harlanitsk/yniemdienanh)
